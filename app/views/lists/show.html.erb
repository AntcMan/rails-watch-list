<h1>
  Here's what's on your <strong><%= @list.name %></strong> list:
</h1>

<ul>
<% @movies.each do |movie|  %>
  <li>
    <%= movie.title %>
    <%= movie.overview %>
    <%= movie.rating %>
    <%# <%= movie.poster_url %>
  </li>
  <% end %>
</ul>

<p><%= link_to "Bookmark Movie",  new_list_bookmark_path(@list), class: 'btn-book' %></p>
